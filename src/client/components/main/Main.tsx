import * as React from 'react';
import MainHeader from '../main-header/MainHeader';

const Main = (props: any) => {
  return (
    <div>
      Main Section
      <MainHeader />
    </div>
  )
}

export default Main;